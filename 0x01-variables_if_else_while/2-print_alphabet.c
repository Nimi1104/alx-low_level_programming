#include <stdio.h>
/**
 * main - entry point
 *
 * Description: Print the lowercase alphabeth
 * Return: 0
 */
int main(void)
{
int c;

for (c = 0; c < 26; ++c)
potchar(('a' + c);

putchar('\n');

return (0);
}
