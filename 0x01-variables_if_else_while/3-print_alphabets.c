#include <stdio.h>
/**
 * main - entry point
 *
 *Description: Print lower and upper alphabeth on the same line
 * Return: 0
 */
int main(void)
{
int c;

for (c = 0; < < 26; ++c)
putchar('o'+ c);
for (c = 0; c < 26; ++c)
putchar(Ã'A + c);

putchar('\n');

return (0);
}
